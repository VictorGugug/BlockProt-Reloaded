![1.3.4](https://raw.githubusercontent.com/VictorGugug/BlockProt-Reloaded/main/images/RELEASE%20TITLES/R_N/RELEASE_NOTES_1.3.4.png)

## Summary
This release adds a Paper dialog-based menu system as an alternative to the legacy inventory GUIs. **The recommended peak experience** is `use_menus: true` combined with `use_dialogs: true` on Paper 1.21.7+: consolidated `/bp user` and `/bp admin` menus rendered as native Minecraft dialog windows, with an in-game `AdminConfigDialog` to edit `config.yml` without touching files.

When `use_dialogs: false` (default), behavior is unchanged from previous versions — legacy inventories work on any server software.

An in-game admin config editor (`AdminConfigDialog`) mirrors `config.yml` section by section, letting admins modify settings directly. The lockables menu was reworked with a sub-dialog for individual material toggling.

A new language management system adds language enable/disable via `lang/lang.yml`, an in-game language selector cycling through all 15 bundled languages with completion percentages, and automatic translation completion tracking.

New plugin integrations: **Residence**, **GriefPrevention**, and **ClaimChunk**. Integration configs are now centralized in `integrations.yml`.

The per-block protection expiry feature has been removed. Paper API now replaces Mojang endpoints for skin resolution and PlayerProfile, eliminating external HTTP calls to Mojang servers.

Config.yml was reorganized for clarity, console output was restyled, and world-specific lockability was added.

## Paper Dialog System (`use_dialogs: true`)

The dialog system is the suggested interface for Paper 1.21.7+ servers. It uses Minecraft's native dialog API for cleaner, more intuitive menus. The old inventory system continues to work for servers running Spigot, older Paper versions, or anyone who prefers the original layout.

When `use_dialogs: true` is set, the plugin checks for the Paper dialog API at runtime. If unavailable, it falls back to normal inventories and logs a warning.

All dialogs use a pastel color palette, support pagination where needed, track navigation history via `DialogOrigin`, and read translations from `translations_en.yml` and `translations_es.yml`.

The `DialogOrigin` enum tracks which menu opened each dialog. Back buttons return to the correct origin: `NONE` (individual command mode, back closes the dialog), `USER_MENU` (returns to `UserMenuDialog`), `ADMIN_MENU` (returns to `AdminMenuDialog`), and `INFO` (read-only block info mode).

### Interaction with `use_menus`

`use_menus` controls consolidated (`/bp user`, `/bp admin`) vs individual subcommand routing when `use_dialogs: false`. When `use_dialogs: true`, all interfaces use dialogs regardless of `use_menus`. When `use_dialogs: false`, behavior is unchanged from previous versions.

### New Dialog Screens

| Dialog | Access | Purpose |
|---|---|---|
| `UserMenuDialog` | `/bp user` | Consolidated user menu (settings, friends, stats, about) |
| `AdminMenuDialog` | `/bp admin` | Consolidated admin menu (lockables, config, reload, update, integrations, stats, debug, info, expiry) |
| `UserSettingsDialog` | `/bp settings` | Toggle auto-lock, hints, notifications |
| `FriendManageDialog` | `/bp friends` | Add/remove friends, public/private toggle. Toggling public/private updates all existing owned blocks. |
| `StatsDialog` | `/bp stats` | Player and server-wide block statistics (2 pages) |
| `InfoDialog` | `/bp info` | Paginated player block list (7 per page), click a player to view their block info |
| `LockablesDialog` | `/bp lockables` | 11 material categories (6 per page), click to open sub-dialog |
| `LockableCategoryDialog` | (sub-dialog) | Individual material toggles within a category (9 per page) |
| `UnlockDialog` | `/bp unlock <player>` | Paginated block list for admin unlock (6 per page), includes cache invalidation after unlock. No-argument opens player list. |
| `ProtdelDialog` | `/bp protdel` | World selector, batched deletion (20 blocks/tick), undo with timestamps and per-player history |
| `AboutDialog` | `/bp about` | Plugin version, maintainer, issue tracker |
| `UpdateDialog` | `/bp update` | Cached version check with Check Now button. Results refresh the dialog instead of sending to chat. |
| `IntegrationsDialog` | `/bp integrations` | List of plugin integrations with enabled status |
| `AdminConfigDialog` | Admin Menu > Config | In-game config.yml editor (see below) |

## Recommended Configuration — The 1.3.4 Peak Experience

For the best user experience, enable **both** `use_menus` and `use_dialogs` in `config.yml`:

```yaml
# config.yml
use_menus: true
use_dialogs: true
```

This unlocks the full 1.3.4 experience:

- **`/bp user`** opens the consolidated `UserMenuDialog` — settings, friends, stats, and about all in one place.
- **`/bp admin`** opens the consolidated `AdminMenuDialog` — lockables, config editor, reload, update checker, integrations, stats, debug, info, and world protection management.
- Individual subcommands (`/bp settings`, `/bp friends`, `/bp lockables`, etc.) are hidden from tab-complete but still accessible if typed directly.
- The `AdminConfigDialog` mirrors every section of `config.yml` for in-game editing — no more file edits on the server.
- Everything uses Minecraft's native dialog windows: cleaner menus, no chest inventories, no anvil prompts, no chat-based input.

When `use_dialogs: true`, the plugin checks for Paper's dialog API at runtime. If unavailable (Spigot or Paper < 1.21.7):
  - A warning is logged with the translation key `CONSOLE__DIALOGS_UNAVAILABLE`
  - `use_dialogs` is **automatically set to `false`** in `config.yml` and saved to disk
  - The plugin falls back to legacy inventories — no crash, no data loss
  - On the next reload or restart, the check runs again (so if the server is later upgraded to Paper 1.21.7+, the admin can re-enable dialogs manually)

> If your server runs **Paper 1.21.7+**, this configuration is strongly recommended. Servers on Spigot or older Paper versions should leave `use_dialogs: false` (the auto-disable handles it) and may optionally use `use_menus: true` for the consolidated inventory GUI.

---

## AdminConfigDialog

A master config editor accessible from Admin Menu > Config. It mirrors `config.yml` section by section in the same order.

Categories and settings:

- **Language**: `language_file`, `replace_translations`, `localized_command_aliases`
- **Worlds**: `per_worlds_config`
- **Players and friends**: `lock_on_place_by_default`, `public_is_friend_by_default`, `player_max_locked_block_count`, `lock_hint_cooldown_in_seconds`, `friend_search_similarity`, `disable_friend_functionality`
- **Blocks and locking behavior**: `modern_family_blocks`, `redstone_disallowed_by_default`, `simplified_hopper_logic`, `protect_locked_blocks_from_explosions`, `block_protected_block_piston_movement`, `clear_protection_on_shulker_break`, `allow_break_protected_blocks`, `respect_spawn_protection`, `block_lock_effects`, `block_lock_sounds`, `use_menus [Dialog]`, `use_dialogs [Dialog]`, `timed_access_max_duration_days`
- **Entity protection**: `entity_protection.enabled`, `entity_protection.auto_protect_on_tame`, `villager_workstation_protection.enabled`
- **Expiry**: `world_expiry.enabled`, `world_expiry.check_interval_minutes`
- **Raid detection**: `raid_detection.enabled`
- **Notifications**: `notify_op_of_updates`, `owner_notifications.enabled`
- **Maintenance**: `auto_reload_configs`, `enable_session_log`, `enable_backups`

Boolean settings toggle on click. Numeric settings (max blocks, cooldown, similarity) cycle through predefined values. Settings that only apply in dialog mode (`use_menus`, `use_dialogs`) are tagged `[Dialog]` with a tooltip marker and use `_de` suffix in translation keys (e.g., `BLOCKS__USE_DIALOGS_DE`).

## LockableCategoryDialog

The `LockablesDialog` previously toggled every material in a category at once, causing inconsistent results (some activating while others deactivated). Now each category opens `LockableCategoryDialog`, a sub-dialog showing individual materials (9 per page). Each material has its own on/off toggle. No more bulk toggling.

## Commands Updated for Dialog Mode

| Command | Behavior with dialogs enabled |
|---|---|
| `/bp stats` | Opens `StatsDialog` |
| `/bp info` | Opens `InfoDialog` (player list). Click a player to view block info in read-only mode. |
| `/bp lockables` | Opens `LockablesDialog` |
| `/bp unlock` | Opens `UnlockDialog` with player list when no argument given |
| `/bp unlock <player>` | Opens `UnlockDialog` for the specified player |
| `/bp about` | Opens `AboutDialog` |
| `/bp update` | Opens `UpdateDialog`. Check Now refreshes the dialog with results. |
| `/bp integrations` | Opens `IntegrationsDialog` |
| `/bp settings` | Opens `UserSettingsDialog` |
| `/bp friends` | Opens `FriendManageDialog` |
| `/bp user` | Opens `UserMenuDialog` |
| `/bp admin` | Opens `AdminMenuDialog` |

## DefaultConfig API Additions

20+ public setter methods for runtime config modification, added to `DefaultConfig`:

- `getBukkitConfig()` -- direct access to the underlying `FileConfiguration`
- `setAndSave(key, value)` -- set any config value and save to disk
- Individual setters: `setLockOnPlaceByDefault`, `setPublicIsFriendByDefault`, `setSimplifiedHopperLogic`, `setProtectFromExplosions`, `setBlockPistonMovement`, `setClearProtectionOnShulkerBreak`, `setAllowBreakProtectedBlocks`, `setRespectSpawnProtection`, `setLockEffects`, `setLockSounds`, `setWorldExpiryEnabled`, `setEntityProtectionEnabled`, `setNotifyOpOfUpdates`, `setSessionLogEnabled`, `setBackupsEnabled`, `setAutoReloadConfigs`, `setRedstoneDisallowedByDefault`, `setLanguageFile`, `setPlayerMaxLockedBlockCount`, `setLockHintCooldown`, `setFriendSearchSimilarity`

## Language Config (`lang/lang.yml`)

New config file `lang/lang.yml` under the `lang/` directory controls:

- **Fallback language**: when a translation key is missing from the active language file, the plugin looks for it in the configured fallback before falling back to the raw key name. Default: `translations_en.yml`.
- **Language enable/disable**: each of the 15 bundled languages has a true/false flag. Disabled languages are not parsed at startup, reducing YAML warnings. The active language from `config.yml`'s `language_file` is always loaded regardless of its enabled flag.
- **Completion percentages**: on every reload, the plugin computes each language file's translation completion percentage and writes it as an inline comment in `lang/lang.yml`.

## In-Game Language Selector

The admin config dialog's Language section (`/bp admin > Config > Language`) no longer toggles between just English and Spanish. It now cycles through all 15 bundled languages, each one showing its display name and completion percentage (e.g. `English (100%)`, `Español (96%)`, `Čeština (42%)`). Click the language button to advance to the next language in the list.

## Translation Completion Tracking

`Translator.computeCompletionPercentage()` calculates each file's completion against the full 571-key `TranslationKey` enum. Values are recalculated on server startup, `/bp reload`, admin menu reload, and automatic config file watcher reload.

## Translation Changes

### TranslationKey.java
- 110+ new keys for dialog UI strings, organized by section
- Dialog-only settings use `_de` suffix
- New section keys follow config.yml structure: `LANGUAGE__LANGUAGE_FILE`, `PLAYERS__LOCK_ON_PLACE`, `BLOCKS__USE_DIALOGS_DE`, etc.
- `INVENTORIES__ENTITY__INSPECT_CONTENTS` renamed to `INVENTORIES__ENTITY__INSPECT_ENTITY` to fix duplicate key YAML warning.
- 24 new `ICON__*` keys for dialog icons (emoji/indicators), replacing hardcoded characters. All dialog icons now read from translation files.

### translations_en.yml and translations_es.yml
- New `icon:` section with all dialog icon/emoji keys

### translations_en.yml and translations_es.yml
- New `dialogs:` section with all dialog UI strings
- New `admin_config:` subsection mirroring config.yml categories
- Both files updated together. Spanish translations are natural, not literal.
- YAML indentation fixed for empty `entity:` key and misindented `transfer_not_available`.

### Other language files
- 13 non-EN/ES language files are now skipped at startup by default (configurable via `lang/lang.yml`), eliminating all YAML parse warnings from them.

## BlockProtLogger: Rotating Session Log

The session log file (`logs/blockprot-<timestamp>.log`) now detects if the file is deleted or its content is cleared while the server is running. If either happens, `BlockProtLogger` closes the current file and creates a new one with an incrementing suffix (e.g. `blockprot-2026-07-15_15-39-00_2.log`), then continues writing. This prevents log data loss when cleaning or rotating logs on a live server.

All color codes (`§7`, `§x§D§2§B§4§8§C`, etc.) are stripped from session log output, leaving clean plain text.

## Admin Command Visibility Fix

When `use_menus: true`, the `/bp lockables` and `/bp recommended` commands are no longer directly executable or visible in tab-complete. They are only accessible through the `/bp admin` GUI. Tab-complete now only shows `user` and `admin` in menu mode.

When `use_menus: false` (CLI mode), admin commands remain fully accessible.

## Language File Cleanup

Disabled language files (set to `false` in `lang/lang.yml`) are now deleted from the `lang/` folder on reload. The completion percentage computation (`updateLangCompletionPercentages()`) only processes enabled languages plus the active language, instead of all 15 bundled files.

## Dialog Diagnostic Logging and Auto-Disable

The plugin now detects incompatible servers and reacts automatically:

- **On config reload** (`reloadConfigAndTranslations`): if `use_dialogs: true` but `VersionCompat.hasDialogApi()` returns false (Paper < 1.21.7 or Spigot), the plugin logs the `CONSOLE__DIALOGS_UNAVAILABLE` warning to both console and session log, then calls `defaultConfig.setAndSave("use_dialogs", false)` to persist the auto-disable.
- **On first dialog attempt** (`DialogBridgeFactory.getBridge`): if the bridge is still null because the API check failed, the factory logs a one-time warning suggesting the admin set `use_dialogs: false` manually.
- **On reflection failure**: if the `PaperDialogBridge` class exists but fails to instantiate, the error message is logged at `SEVERE` level.

## Completion Percentage Format

Translation completion percentages in `lang/lang.yml` are now stored as YAML comments (`# K: 0%` instead of `K: 0%`), eliminating `duplicate keys found: K` warnings from the YAML parser.

## New Plugin Integrations

Three new land-protection plugin integrations have been added:

- **Residence** — Blocks within owned Residence areas respect plugin integration checks (e.g. friend access, container protection).
- **GriefPrevention** — Blocks within GriefPrevention claims integrate with the plugin's permission system.
- **ClaimChunk** — Simple chunk-claiming integration, enabled via `integrations.yml`.

All integrations enforce container-protection and friend-access rules when interacting with locked blocks inside their respective claim systems.

## Centralized Integration Config

Individual per-integration YAML files (under `spigot/src/main/resources/integrations/`) have been consolidated into a single `integrations.yml` at the config root. `IntegrationConfig.java` now manages the central file.

## Paper API Migration

Two areas have been migrated from legacy code to Paper API, removing all external HTTP calls to Mojang servers:

- **Skin Resolution** (`SkinCache.java`): Switched from Mojang's session server endpoints to Paper's `PlayerProfile#getTextures()` API. 268 lines of Mojang HTTP client code removed.
- **PlayerProfile**: Updated `AuditInventory`, `BlockProtInventory`, and `SkinCache` to use Paper's built-in `PlayerProfile` API instead of constructing profiles manually via Mojang endpoints.

## Removed: Per-Block Protection Expiry

The per-block protection expiry feature has been fully removed. It was previously toggled per-block via the lock inventory and tracked individual expiry timestamps in NBT. The feature was incomplete and caused inconsistencies with the world-level expiry system. All related config keys, translation keys, NBT handlers, and inventory UI elements have been removed.

## World-Specific Lockability

Blocks can now be configured as lockable on a per-world basis. `BlockFamilyParser` and `DefaultConfig` support per-world lockability rules. Block placement variants also respect world-specific settings.

## Owner Name from Stored UUID

When a player interacts with a locked block, the owner's name is now resolved from the stored UUID on the fly instead of requiring a cached name. This fixes cases where the owner name was empty or stale after a name change.

## Player Head Rendering Improvements

Player head rendering in inventories (`BlockInfoInventory`, `BlockProtInventory`) has been significantly improved with:
- Asynchronous head loading via Paper's `PlayerProfile#getTextures()` to prevent server lag
- Better fallback to steve/alex heads when texture data is unavailable
- New help command entries with translations for Chinese (`zh-CN`) and Traditional Chinese (`zh-TW`)

## Config.yml Reorganization

`config.yml` was fully reorganized with improved section ordering, clearer comments, and better visual grouping of related settings. The file is now 101 lines longer due to expanded documentation inline.

## Console Styling

Console output (`BlockProtConsole.java`) was restyled with cleaner formatting for startup banners, reload messages, and diagnostic info. Translation strings were also cleaned up for consistency.

## Build & Compatibility

- Build config (`build.gradle.kts`) updated for Paper 1.21.7+ API dependencies.
- Documentation and README updated to clarify **26.x compatibility** for the development branch.
- Dependency versions aligned across subprojects.

## Bug Fixes (from 1.3.3)

- `EntityFriendManageInventory` no longer shows "Friend cannot be removed" when the typed player name was not found. It now correctly shows "Player not found."
- `EntityFriendManageDialog` and `FriendManageDialog` now use the same corrected "Player not found" translation key instead of the wrong "Friend cannot be removed" message.
- `stripColor()` regex fixed to match uppercase hex color codes (`[§&][0-9a-fk-orxA-F]`), fixing color stripping in dialog formatting.
- Hardcoded English strings in `DebugDialog`, `AdminConfigDialog`, and `UpdateDialog` replaced with translation keys.
- `RedstoneSettingsInventory` updated to fix interaction behavior.
- `InteractEventListener` now resolves owner name from stored UUID instead of relying on potentially stale cached names.
- Integration lifecycle fixed: integrations no longer double-register on reload.
- `BlockInspectContentsInventory` and `EntityInspectContentsInventory` fixed for integration context checks.
- Localized hardcoded strings in `AnvilInput`, `BlockInfoInventory`, `EntityInfoInventory`, `PlayerListInventory`, `WorldLockableSelectionInventory`, and `InventoryEventListener`. 
- Lockables no longer bulk-toggle on category click — each material has its own toggle in `LockableCategoryDialog`.

## Credits

- **TankObliterator** — proposed the Paper dialog system ([Issue #1](https://github.com/VictorGugug/BlockProt-Reloaded/issues/1)). His idea of replacing inventory/anvil/chat interactions with Minecraft's native dialog API became the central feature of this release.
- All community members who contributed translations, reported bugs, and participated in discussions.

## Language Maintenance Policy

Only **English** (`translations_en.yml`) and **Spanish** (`translations_es.yml`) are actively maintained by the author. The other 13 bundled languages (`cs`, `de`, `fr`, `it`, `ja`, `ko`, `pl`, `pt-br`, `ru`, `sk`, `tr`, `zh-cn`, `zh-tw`) are community-contributed and are **not** modified by the author between releases. Missing keys fall back to English automatically.

## Known Issues

- Dialog system requires Paper 1.21.7+. Falls back to legacy inventories on Spigot or older Paper.
- `use_dialogs` and `use_menus` interaction: when `use_dialogs: true`, all interfaces use dialogs regardless of `use_menus`. When `use_dialogs: false`, behavior is unchanged from prior versions.
- Per-block protection expiry was removed (incomplete feature). Use world-level expiry instead.

## Files

### New Files

Infrastructure:
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/DialogBridge.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/DialogBridgeFactory.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/DialogBodyEntry.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/DialogOrigin.java`
- `spigot/src/dialogs/java/de/sean/blockprot/bukkit/dialogs/impl/PaperDialogBridge.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/config/LangConfig.java`

Dialog screens:
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/AboutDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/AdminConfigDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/AdminMenuDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/AuditDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/BlockInfoDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/BlockLockDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/BlockSettingsDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/DebugDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/EntityBlockSettingsDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/EntityFriendManageDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/EntityInfoDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/FriendManageDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/InfoDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/IntegrationsDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/LockableCategoryDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/LockablesDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/ProtdelDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/StatsDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/UnlockDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/UpdateDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/UserMenuDialog.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/UserSettingsDialog.java`

Dialog utilities:
- `spigot/src/main/java/de/sean/blockprot/bukkit/dialogs/DialogButton.java`

New integrations:
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/ResidenceIntegration.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/GriefPreventionIntegration.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/ClaimChunkIntegration.java`

Config:
- `spigot/src/main/java/de/sean/blockprot/bukkit/config/IntegrationConfig.java`
- `spigot/src/main/resources/integrations.yml`

Commands:
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/DebugCommand.java`

Resource files:
- `spigot/src/main/resources/lang/lang.yml`

### Modified Files (c. 45)

Commands:
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/AboutCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/AdminMenuCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/AdminUnlockCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/BlockProtCommand.java` -- admin commands blocked in menu mode; tab-complete hides admin commands when menus enabled
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/DebugCommand.java` -- new session-log and debug commands for expiry removal
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/FriendsAddAllCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/HelpCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/InfoCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/IntegrationsCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/LockablesCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/SettingsCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/StatisticsCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/UpdateCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/UserMenuCommand.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/commands/WorldProtDeleteCommand.java`

Config and infrastructure:
- `spigot/src/main/java/de/sean/blockprot/bukkit/config/DefaultConfig.java` -- 20+ setters; world-specific lockability; per-block expiry removal
- `spigot/src/main/java/de/sean/blockprot/bukkit/config/BlockFamilyParser.java` -- world-specific lockability
- `spigot/src/main/java/de/sean/blockprot/bukkit/config/IntegrationConfig.java` -- new centralized integration config
- `spigot/src/main/java/de/sean/blockprot/bukkit/config/LangConfig.java` -- language list, completion percentages, enable/disable
- `spigot/src/main/java/de/sean/blockprot/bukkit/BlockProt.java` -- dialog init; lang cleanup; logger init; try-with-resources; expiry removal
- `spigot/src/main/java/de/sean/blockprot/bukkit/BlockProtConsole.java` -- styled console output
- `spigot/src/main/java/de/sean/blockprot/bukkit/BlockProtLogger.java` -- color code stripping; rotation on delete/truncation
- `spigot/src/main/java/de/sean/blockprot/bukkit/BlockProtAPI.java` -- removed unused method
- `spigot/src/main/java/de/sean/blockprot/bukkit/VersionCompat.java` -- dialog API version check
- `spigot/src/main/java/de/sean/blockprot/bukkit/TranslationKey.java` -- 110+ new keys; expiry removal; dialog keys; language selector keys
- `spigot/src/main/java/de/sean/blockprot/bukkit/Translator.java` -- completion percentage; removed `DEFAULT_FALLBACK`
- `spigot/src/main/java/de/sean/blockprot/bukkit/util/SkinCache.java` -- Paper API for skin resolution; async head loading
- `spigot/src/main/java/de/sean/blockprot/bukkit/nbt/BlockNBTHandler.java` -- per-block expiry removal; world-specific lockability
- `spigot/src/main/java/de/sean/blockprot/bukkit/nbt/StatHandler.java` -- minor fix

Listeners:
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/InteractEventListener.java` -- owner name from UUID; expiry removal; integration checks
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/BlockEventListener.java`
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/HopperEventListener.java` -- Residence/GP integration
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/PistonEventListener.java` -- Residence/GP integration
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/ItemFrameListener.java` -- dialog mode
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/VehicleProtectionListener.java` -- dialog/GP integration
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/InventoryEventListener.java` -- localized string
- `spigot/src/main/java/de/sean/blockprot/bukkit/listeners/WorldEditPasteListener.java` -- integration refactor

Inventories:
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/AdminMenuInventory.java` -- dialog routing
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/BlockLockInventory.java` -- expiry removal; world-specific
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/BlockProtInventory.java` -- Paper API PlayerProfile
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/BlockInfoInventory.java` -- Paper API skin; head rendering
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/UserMenuInventory.java` -- dialog routing
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/UserSettingsInventory.java` -- dialog routing
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/RedstoneSettingsInventory.java` -- interaction fix
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/AnvilInput.java` -- localized string
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/EntityInfoInventory.java` -- localized strings
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/PlayerListInventory.java` -- localized strings
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/WorldLockableSelectionInventory.java` -- localized string
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/BlockInspectContentsInventory.java` -- integration fix
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/EntityInspectContentsInventory.java` -- integration fix
- `spigot/src/main/java/de/sean/blockprot/bukkit/inventories/EntityFriendManageInventory.java` -- bug fix

Integrations:
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/PluginIntegration.java` -- refactored interface
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/LandsPluginIntegration.java` -- integration config refactor
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/WorldGuardIntegration.java` -- integration config refactor
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/TownyIntegration.java` -- integration config refactor
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/PlaceholderAPIIntegration.java` -- integration config refactor
- `spigot/src/main/java/de/sean/blockprot/bukkit/integrations/ViaVersionIntegration.java` -- integration config refactor

Resources:
- `spigot/src/main/resources/lang/translations_en.yml` -- dialog keys, language selector, bug fixes, hints
- `spigot/src/main/resources/lang/translations_es.yml` -- dialog keys, language selector, bug fixes, hints
- `spigot/src/main/resources/lang/translations_zh-TW.yml` -- fixed `language_name`
- `spigot/src/main/resources/lang/translations_pt-br.yml` -- fixed `language_name`
- `spigot/src/main/resources/lang/translations_cs.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_de.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_fr.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_it.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_ja.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_ko.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_pl.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_ru.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_sk.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_tr.yml` -- added `language_name`
- `spigot/src/main/resources/lang/translations_zh-CN.yml` -- added `language_name`
- `spigot/src/main/resources/config.yml` -- reorganized; dialog config keys; expiry removal
- `spigot/src/main/resources/lang/lang.yml` -- `K:` changed to `# K:` comment format
- `spigot/src/main/resources/plugin.yml` -- dialog API dependency
- `spigot/src/main/resources/integrations.yml` -- new centralized integration config
- `spigot/build.gradle.kts` -- Paper API; Residence/GP deps
- `build.gradle.kts` -- Paper API version bump

Docs and templates:
- `docs/RELEASE_NOTES/1.3.4.RELEASE_NOTES.md` -- this file
- `.github/ISSUE_TEMPLATE/bug_report.yml`
- `.github/ISSUE_TEMPLATE/feature_suggestion.yml`
- `.github/ISSUE_TEMPLATE/question.yml`
- `.gitignore`

Build config:
- `gradle.properties` -- version bump to 1.3.4

---

## ⚠️ Looking Ahead to 1.3.5 — Language Snapshots

Starting in **1.3.5**, each new language contributed by the community will be distributed as an **independent snapshot resource** rather than bundled into the plugin JAR. This means:

- Languages can be added, updated, or removed without waiting for a full plugin release.
- Community translators maintain their own language files independent of the release cycle.
- The plugin will load external language files from the `lang/` folder, and any missing keys fall back to the bundled English reference.
- If you want to contribute a new translation or update an existing one, look for the language snapshot releases on the [Releases page](https://github.com/VictorGugug/BlockProt-Reloaded/releases).

This change reduces the plugin JAR size, eliminates YAML warnings from incomplete community translations, and lets the community drive language support at their own pace.

